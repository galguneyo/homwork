# 과제 제출 — 나만의 에이전트 만들기

## HCM Release Radar

**Workday HCM과 SAP SuccessFactors의 반기 릴리스를 추적·비교하고, 한국 고객 관점의 적용 방안까지 산출하는 컨설팅 리서치 에이전트**

| | |
|---|---|
| 제출자 | shawn.jihoon.joo@gmail.com |
| 저장소 | `galguneyo/homwork` · 브랜치 `claude/eager-heisenberg-4wulfm` |
| 에이전트 | `.claude/skills/hcm-release-radar/` · `.claude/agents/hcm-release-radar.md` |
| 산출물 ① | `output/2026-09-16_workday-2026R2_vs_sfsf-2H2026.md` — 반기 브리핑 |
| 산출물 ② | `dashboard/index.html` — 2021–2027 인터랙티브 대시보드 |
| 데이터 | 마일스톤 56건 · 릴리스 웨이브 28건 · 기능영역 14개 · 인사이트 7건 · Practice 테마 7개 |

---

## 1. 왜 이 에이전트인가

HR 컨설팅에서 두 벤더의 릴리스 추적은 **반기마다 반복되는데, 매번 처음부터 다시 합니다.** 그 결과 비싼 작업(판단)이 싼 작업(수집)에 묻힙니다.

더 큰 문제는 **정확도**입니다. 이 산출물은 제안서와 고객 보고에 들어갑니다. 없는 기능 이름 하나, 틀린 폐기 일자 하나가 벤더 미팅에서 정정당하면 그 자리의 신뢰가 사라집니다. **일반적인 리서치 에이전트는 빈칸을 그럴듯한 문장으로 메우는 경향이 있고, 이 도메인에서 그건 치명적입니다.**

그래서 이 에이전트는 **"많이 찾는 것"이 아니라 "지어내지 않는 것"을 1차 목표로 설계**했습니다.

## 2. 무엇을 만들었나

### 2.1 구조

```
.claude/
  agents/hcm-release-radar.md          # 서브에이전트 정의 (위임 실행용)
  skills/hcm-release-radar/
    SKILL.md                           # 실행 절차 (/hcm-release-radar)
    references/
      evidence-protocol.md             # 출처 등급 · 날조 방지 규칙 · 비대칭 규칙
      source-registry.md               # 1차 출처 16건, 등급·접근성 명시
      module-crosswalk.md              # Workday↔SFSF 기능·SKU 대칭 스파인 (16행)
      output-template.md               # 산출물 템플릿 (9개 섹션)
      korea-advisory-plays.md          # 한국 고객 활용안 작성 규칙 (5필드 강제)
data/
  hcm-timeline-2021-2026.json          # 마일스톤 + 릴리스 웨이브 (단일 진실원)
  insights-ko.json                     # 인사이트 + 한국 HR Practice
scripts/build-dashboard.js             # 데이터 주입 + 무결성 검사
dashboard/template.html → index.html   # 대시보드
output/                                # 반기 브리핑
```

**스킬과 서브에이전트 두 형태로 제공**합니다. 스킬은 대화 안에서 절차를 따라 실행할 때, 서브에이전트는 백그라운드로 위임할 때 씁니다.

### 2.2 핵심 설계 — 날조 방지 3원칙

이 에이전트의 차별점은 기능이 아니라 **제약**입니다.

**① 티어 태깅** — 모든 사실 라인에 출처 등급과 URL을 답니다.

| 등급 | 의미 |
|---|---|
| `T1` | 벤더가 직접 쓴 공개 문서 |
| `T1*` | 벤더 페이지를 지목했으나 원문을 열지 못하고 검색 요약으로 읽음 |
| `T2` | 구축 파트너 · SI |
| `T3` | 미디어 · 애널리스트 · 커뮤니티 개인 글 |

날짜는 T1이거나 독립 T2 2건이 일치할 때만 단정합니다. 출처가 충돌하면 **더 나은 숫자를 고르지 않고 충돌 사실을 남깁니다.**

**② Tier-1-or-silence** — 근거가 없으면 벤더 마케팅 문구로 칸을 채우지 않고 `문서 비공개 / 미확인`으로 둡니다. 산출물 끝에는 **미확인 항목 목록을 반드시 붙입니다.** 정직한 공백 목록이 완전해 보이는 리포트보다 낫다는 전제입니다.

**③ 비대칭 규칙** — Workday는 기능 단위 릴리스 노트를 Community 로그인 고객에게만 공개하고, SAP는 What's New Viewer를 공개합니다. 따라서 **어느 반기든 공개 기록의 깊이는 SAP 쪽이 항상 더 깊습니다.** 이건 문서 정책의 산물이지 릴리스 내용의 차이가 아니므로, 공개 자료의 양을 근거로 벤더를 비교하는 문장을 금지하고 매 산출물에 이 사실을 명시하게 했습니다.

추가로, 컨설턴트 해석은 전부 `[가설]`로 표시하고 **선행조건과 반증 테스트**를 함께 답니다 — 가설이 벤더 주장으로 세탁되는 것을 막는 장치입니다.

## 3. 산출물

### 3.1 반기 브리핑 (`output/`)

9개 섹션: 한 장 요약 · 방법론과 신뢰도 · 릴리스 캘린더 · 로드맵 특이사항 · SKU 패키징 대칭 · 기능영역별 EN/KO 대칭 비교 15건 · 폐기 일정 · 열람 가능 자료 · 한국 고객 활용안 8건 · **미확인 항목 11건**.

### 3.2 대시보드 (`dashboard/index.html`)

| 섹션 | 내용 |
|---|---|
| 01 릴리스 리듬 | 14개 반기 축. 일자 확인 여부를 표식 모양으로 인코딩(속 참/빔), 미발표 구간은 별도 음영 |
| 02 시점 × SKU 매트릭스 | 14개 기능영역 × 7년. 선택 시 우측 드로어에 EN 원문·KO 해설·SKU·출처 |
| 03 고객 상황 진단 | 고객 상황 입력 → 데이터셋 안에서만 근거 검색 |
| 04 인사이트 | 6년 이력에서 도출한 7건, 각각 근거 마일스톤으로 연결 |
| 05 한국 HR Practice | 7개 테마 Benefit / Consideration |
| 06 방법론과 한계 | 출처 등급 · 비대칭 고지 · 수집 제약 · 남은 공백 |

### 3.3 고객 상황 진단 — 이 과제에서 가장 신경 쓴 부분

컨설턴트 교육용이므로 **블랙박스 답변이 아니라 판단 경로를 보여주는 구조**로 만들었습니다.

- **검색은 결정적**입니다 — 15개 기능영역 키워드 사전 + 5개 상황 렌즈로 입력을 파싱하고 마일스톤에 점수를 매깁니다. **존재하지 않는 항목이 구조적으로 나올 수 없습니다.**
- **매칭 근거를 노출**합니다 — 어떤 단어가 어떤 영역에 걸렸는지 신호 칩으로 함께 보여줍니다.
- **빈 결과를 그대로 보여줍니다** — 매칭이 없으면 "근거 없음"을 출력하고 억지로 채우지 않습니다.
- **AI 종합 소견은 선택**입니다(`sample` capability). 매칭된 근거만 프롬프트에 담고 *"제공되지 않은 기능명·날짜·수치를 만들지 말 것, 근거에 없으면 미확인으로 표기할 것"* 을 지시합니다.

→ **검색은 결정적이고 합성만 AI**인 구조라, LLM이 릴리스 사실을 지어낼 여지를 차단했습니다.

## 4. 품질 관리

산출물이 제안서에 들어가므로, 눈으로 보는 검증에 의존하지 않고 **기계적 게이트**를 걸었습니다.

| 게이트 | 내용 |
|---|---|
| 빌드 무결성 검사 | ID 중복 · 필수 필드 누락 · 미정의 SKU 키 · URL 아닌 출처 · 창 밖 일자 · **근거 없는 인사이트** · 웨이브 미커버 연도 → 하나라도 걸리면 **발행 대신 실패** |
| 색상 검증 | 범주형 팔레트를 스크립트로 검증 — 명도 대역 · 채도 하한 · 색각 분리(ΔE) · 배경 대비 **전 항목 통과** |
| 렌더 검증 | 헤드리스 브라우저로 1512 / 1280 / 1181 / 1100 / 768 / 390px 측정 — 정렬 좌표, 가로 스크롤 0, 콘솔 에러 0 |

작업 중 실제로 잡은 버그 예: `<section id="matrix">`와 `<table id="matrix">`의 **ID 중복**으로 `getElementById`가 섹션을 잡아 필터·드로어가 통째로 지워지던 문제, `#tagChips` 위임 핸들러가 새 칩을 태그로 처리해 **필터 해제 시 결과가 0건**이 되던 문제.

## 5. 실행 방법

```bash
# 반기 브리핑 생성
/hcm-release-radar

# 또는 서브에이전트로 위임
Agent(subagent_type="hcm-release-radar", prompt="2026H2 wave 갱신")

# 대시보드 재빌드 (데이터 수정 후)
node scripts/build-dashboard.js
```

**다음 반기 갱신**은 JSON에 릴리스 웨이브와 마일스톤을 추가하고 빌드하면 끝입니다. 연도는 어디에도 하드코딩되어 있지 않고 — 차트 축·연도 헤더·본문 문구·통계가 전부 데이터에서 파생되며 "오늘" 표식은 열람 시점에 계산됩니다 — 2027년 이후에도 그대로 동작합니다.

## 6. 한계 — 명시적으로 남깁니다

1. **수집 환경 제약** — 작업 환경의 egress 정책이 벤더 도메인(공개 문서인 `doc.workday.com` 포함) 직접 열람을 차단했습니다. 모든 근거를 검색 요약으로 수집했고, 그에 따라 **T1 근거를 전부 `T1*`로 강등** 표기했습니다. 고객 배포 전 원문 확인이 필요합니다.
2. **Workday 기능 단위 목록 부재** — Community 로그인이 필요합니다. 본 과제에서는 자격증명 우회를 시도하지 않았습니다(계약·상표 리스크).
3. **마일스톤 수 ≠ 벤더 출시량** — 두 벤더 모두 반기당 250여 건을 내놓으며, 이 데이터셋은 구조·전략을 바꾼 변화만 선별 수록합니다.
4. **한국 활용안의 맥락** — 사용자의 실제 과거 대화 기록에 접근할 수 없어, 워크스페이스에 영속화된 프로젝트 스킬에서 대용으로 도출했습니다(브리핑 §8에 명시).

미확인 항목 11건은 산출물과 대시보드 양쪽에 목록으로 남아 있습니다.

## 7. 평가자가 30초에 확인할 것

1. **대시보드 §06 방법론** — 한계를 숨기지 않고 먼저 말하는지
2. **§03 고객 상황 진단에 "오늘 날씨가 좋네요" 입력** — 근거가 없을 때 지어내는지, 빈 결과를 내놓는지
3. **`.claude/skills/hcm-release-radar/references/evidence-protocol.md`** — 제약이 문서로 존재하는지
4. **`scripts/build-dashboard.js`** — 검증이 코드로 강제되는지
